# Prompt: local LLM stack installer for macOS

Numbered so sections can be amended or appended without rewriting. Add new requirements under the matching section, or under §15.

---

## 1. Objective

Write a single, shareable shell script that provisions a private, self-hosted LLM stack on macOS, plus a GitHub-style `README.md` documenting it, and a GitHub Actions CI workflow that lints and tests the script.

## 2. Target environment

- macOS, Apple Silicon (arm64) only. Refuse to run elsewhere, in preflight, with a clear message.
- zsh as the user shell.
- Assume a fresh OS install. Assume no console login — the machine is administered over SSH.
- Make no assumption about username or home directory path. Use `$HOME` and `id -un` throughout. Do not use `whoami`; it is deprecated on modern macOS. Use `id -un` instead, which is the POSIX-recommended replacement.
- Contain nothing specific to my network. Anything site-specific must be a flag or config value.

## 3. Deliverables

1. `llmstack-macos.sh` — one file, executable, no companion scripts.
2. `README.md` — GitHub conventions: contents list, tables, fenced blocks with language hints.
3. `.github/workflows/ci.yml` — GitHub Actions workflow that lints and tests the script (see §15).
4. `.shellcheckrc` — shellcheck configuration to suppress known false positives.
5. A model catalogue file, generated by the script on first run if absent, never overwritten afterwards.

## 4. Stack

| Component | Purpose |
|---|---|
| Ollama | inference engine |
| Open WebUI | web front-end |
| SearXNG | private web search for Open WebUI |
| Draw Things | image/video generation, optional |

Install Homebrew, Python 3.11, and a container runtime only as needed. Prefer pip/venv over containers for Open WebUI.

## 5. Service management

- Ollama and Open WebUI run as **system LaunchDaemons** in the `system` domain, so they start at boot with no login session.
- Privilege-drop both to the invoking user via `UserName`. Never run them as root.
- **Auto-login must never be required, suggested, or enabled.** This is non-negotiable; design around it.
- Validate every generated plist with `plutil -lint` before loading, and fail loudly if malformed.
- Set `HOME` explicitly in each daemon's environment. Daemons do not inherit it reliably.
- Set plist ownership to `root:wheel` and mode `0644` after writing, matching macOS conventions for system LaunchDaemons. Do not assume `sudo tee` leaves correct ownership.
- The `WEBUI_SECRET_KEY` is passed to the Open WebUI daemon via the plist's `EnvironmentVariables` dict. System LaunchDaemon plists are world-readable (0644). Document this security tradeoff in `--help` and in the README under a Security section. On a single-user Mac this is fine; on a multi-user machine, suggest alternatives (shell profile environment, file-based key, or restricted permissions).

## 6. Modes and CLI

Modes: `--install` (default), `--update` (alias `--upgrade`), `--status`, `--recommend`, `--uninstall`, `--version`, `--help`.

Options: `--searxng-url URL`, `--searxng-port PORT`, `--webui-port PORT`, `--model TAG`, `--no-model`, `--no-drawthings`.

- `--help` is comprehensive: synopsis, every mode and option, components, file layout, ports, startup behaviour *and its limits*, security note about the plist secret key, requirements, post-install steps, exit status, examples.
- `--version` prints the script name and version, then exits 0.
- `--recommend` and `--status` are read-only. They must install, download, and change nothing.
- `--uninstall` confirms **each artifact separately**. Every prompt defaults to no; bare Enter skips. Destroying user data takes two confirmations.
- `--uninstall` never removes shared dependencies (Homebrew, Python, `mas`). Say so in the summary.
- `--update` backs up user data before touching anything, and restarts the stack even if an upgrade step fails.
- All port arguments must be validated as integers in the range 1–65535 before anything is installed. Reject non-numeric, zero, negative, and out-of-range values immediately with a clear error.
- Before installing, check whether the target ports (`--webui-port`, `--searxng-port` in local mode) are already in use. Warn clearly if they are, naming the conflicting port and the process that holds it (use `lsof` to identify the holder). Suggest `--webui-port` or `--searxng-port` to choose another. Do not abort — the user may be replacing a previous install — but warn so a silent failure is not the first symptom.

## 7. Model selection

- Detect chip, tier (Base/Pro/Max/Ultra), memory, and free disk.
- Budget roughly 70% of unified memory for weights.
- Select against a catalogue on **two independent gates**: size must fit the budget, *and* the entry's declared minimum machine class must be met. Fitting and running well are different things — a dense 70B fits a 64 GB budget and performs badly on Pro-tier bandwidth.
- Prefer MoE over dense where both fit. Generation is memory-bandwidth-bound; MoE activates a fraction of parameters per token.
- Fall back to a lighter tier rather than recommending nothing.
- Warn when free disk is short of the model size plus headroom.
- **Use only real, currently-available model tags.** Verify every tag against the Ollama registry (https://ollama.com/library) before marking it `yes` in the VERIFIED column. Do not invent or speculate about model names, sizes, or tags. If unsure, mark the tag `no` and note it. Tags that don't exist will fail to pull and undermine trust in the script.
- A regression guard is valuable: the CI workflow must check that known-fictional tags (e.g. `qwen3.6:35b-a3b`, `gemma4:26b-a4b`, `llama3.3:70b`) do not appear in the script or the generated catalogue.

## 8. Catalogue file

- Plain text, pipe-delimited, `#` comments, greppable and hand-editable.
- Fields: minimum RAM, tag, size, architecture, role, verified flag, notes.
- Header carries `# Last-Updated: YYYY-MM-DD`.
- The script parses that date and grades staleness: recent under 90 days, worth reviewing at 90–180, very likely stale beyond 180. Report this in `--update` and `--recommend`.
- Mark each tag verified or unverified. **Mark a tag verified only if you actually confirmed it against the Ollama registry in this session.** Warn before pulling anything unverified; on pull failure, point at the library and continue rather than aborting.
- Include entries across multiple roles: daily, coding, vision, light. Ensure at least one verified daily-driver entry exists.
- The CI workflow must validate the catalogue format: 7 pipe-delimited fields per data line, VERIFIED column is `yes` or `no`, ARCH column is `moe` or `dense`, ROLE column is one of the known values, and at least one verified daily driver is present.

## 9. Platform constraints to handle, not discover

macOS container runtimes require an active GUI login session. Docker Desktop is a GUI app and cannot be launched over SSH; Colima manages a per-user VM and is unsupported as a system daemon. Therefore:

- Use Colima, not Docker Desktop, when a runtime is needed.
- Local SearXNG cannot survive a reboot without a console login. State this plainly in `--help`, the README, and `llmstatus` output. Do not paper over it.
- Offer `--searxng-url` to delegate search to a Linux host where Docker is a real systemd service, and skip installing any runtime in that mode.

## 10. Failure modes to pre-empt

Each of these has bitten a working deployment. Handle them in code, not in prose.

1. `WEBUI_SECRET_KEY` must be hex only. Base64 values containing `+` or `=` are silently dropped from a plist environment. Generate with `secrets.token_hex(16)`, persist to a `0600` file, reuse on re-runs.
2. Set `SoftResourceLimits` and `HardResourceLimits` to 65536 for Open WebUI, or web search dies with `Too many open files`.
3. Pin `DATA_DIR` explicitly and consistently. A mismatch silently creates an empty database and looks exactly like wiped credentials.
4. Docker creates a **directory** at any bind-mount path that doesn't yet exist. Detect and remove a stray `settings.yml` directory, and write the config file before starting the container.
5. The SearXNG container listens on 8080 internally regardless of its config. Map host-port to 8080.
6. `launchctl bootstrap` fails with error 5 when already loaded. Always `bootout` first, wait, then bootstrap, and treat a bootstrap error as non-fatal after verifying with `launchctl print`.
7. Check port availability before binding; report the conflicting holder — print the process name and PID from `lsof`, not just that the port is busy.
8. SearXNG needs `json` in its `formats` list or Open WebUI receives nothing. Document this in the README troubleshooting section.
9. A shell block appended to `.zshrc` on every re-run collides with itself. Guard on **every** marker string the tooling has ever written, not just the current one, and *replace* the block rather than leaving an existing one in place. Definitions of the same name that changed form between versions — an alias becoming a function — are fatal in zsh, not merely redundant: zsh expands a live alias while parsing a function of that name and the parse fails. Warn about definitions found outside any marked block instead of silently duplicating them, and tell the user to `exec zsh`, since sourcing cannot clear what is already in the running shell.
10. **Error trap during install.** If the script exits unexpectedly during the install phase, the user is left with partial state and no guidance. Trap `EXIT` during install, and on non-zero exit print a summary showing the exit code, where to find the daemon logs (Ollama and Open WebUI error log paths), and a reminder that re-running is safe because the script is idempotent. Clear the trap and reset the flag once install completes normally.
11. **SIGINT during model pull.** `ollama pull` of a large model can take a long time and the user may Ctrl-C. Trap `INT` around the pull, print a clear message that the pull was interrupted and can be resumed by re-running, then exit 1. Clear the trap after the pull completes or fails.
12. **Hanging `curl` calls.** Every `curl` call used for status checks or readiness polling must set `--max-time` (2–5 seconds). Without it, a hung service causes the script to block indefinitely with no output.
13. **`WEBUI_BIND` must be persisted.** The bind address is used in the plist at install time, but the shell functions in `.zshrc` also need to know it (for status checks, for display). Write `WEBUI_BIND` to the config file alongside the other settings so the shell functions read it at call time.
14. **Status logic divergence.** The code that checks component health runs in two places: the script's `--status` mode and the `llmstatus` shell function emitted into `.zshrc`. These must share a single code block (a string variable like `STATUS_BODY`) that is both `eval`'d by the script and written verbatim into `.zshrc`. Do not copy-paste the logic; copy-pasted duplicates drift apart and report different things.
15. **Plist ownership.** `sudo tee` writes the file but does not guarantee `root:wheel` ownership. Explicitly `sudo chown root:wheel` and `sudo chmod 644` each plist after writing it.

## 11. Script quality

- `set -euo pipefail`. Fully idempotent: every step checks state before acting; existing data is never overwritten.
- Poll for readiness against real endpoints with `curl --max-time`. No fixed `sleep` as a substitute for a check, except short pauses around `launchctl` state transitions which are inherently asynchronous.
- Long or large operations print progress; downloads state expected size up front.
- Never `source` an interactive rc file from inside the script.
- Delimit any `.zshrc` block with **both** start and end markers, keep a list of markers used by previous versions, and remove every match before writing a fresh block. Back the file up first. Match markers literally using `awk index()`, not as regexes — marker text containing `(`, `)`, or `.` breaks a `sed` address.
- Never edit a user's shell config destructively without a backup, and never delete a range whose extent you cannot determine — report it and skip.
- Shell functions read settings from a config file at call time rather than having values baked into `.zshrc`.
- **Color output.** Auto-detect whether stdout is a terminal with `[ -t 1 ]` and `tput`. If yes, use bold, red, yellow, and green for log/warn/error/ok messages. If no (piped, redirected, or dumb terminal), emit plain text. Never emit raw ANSI codes unconditionally.
- **`find` with `-print0`.** Any `find` command whose results are iterated must use `-print0` and `read -r -d ''` to handle paths with spaces. Never pipe `find` output through a plain `while read` loop.
- Comment *why*, not *what*, especially at each workaround from §10.

## 12. Documentation

`README.md` must include: what it installs, requirements, quick start, modes and options tables, how model selection works and why bandwidth matters, the catalogue format and date mechanism, the reboot limitation with its cause, remote-SearXNG setup including a working Compose file, shell commands, updating, uninstalling, file layout, ports, a **Security section** covering the plist secret-key exposure tradeoff and plist ownership, a troubleshooting section covering every item in §10 with the diagnostic command for each, an FAQ, design notes explaining the contested choices, and a **Changelog** section listing changes by version.

## 13. Working style

- State assumptions and design decisions before writing code. Where options exist, name them, weigh the tradeoffs, and justify the choice.
- Push back on anything that adds risk or unnecessary complexity rather than complying silently.
- Flag uncertainty rather than hiding it. Distinguish verified fact from inference.
- Verify current facts — package names, image tags, model tags, API endpoints — rather than relying on recall. Say which ones you verified and which you didn't. **Never invent model tags.** If a tag cannot be confirmed, mark it unverified or omit it.
- Syntax-check the script and test any non-trivial parsing logic against representative inputs before delivering.
- Deliver complete files, never diffs or fragments.

## 14. CI workflow

Provide `.github/workflows/ci.yml` and `.shellcheckrc` that:

### Lint job (Ubuntu, fast)
- `bash -n` syntax check.
- `shellcheck -x` with the `.shellcheckrc` suppressing `SC1091` (non-constant source), `SC2154` (false positives from `eval`'d `STATUS_BODY`), `SC2317` (unreachable command false positives in trap/eval blocks).
- Verify the shebang is `#!/bin/bash`.

### CLI test job (Ubuntu)
- `--help` and `-h` exit 0; `--help` mentions every mode including `--version`.
- `--version` exits 0 and prints a version number.
- Unknown argument exits 1.
- Invalid ports (non-numeric, zero, negative, out of range) exit 1.
- Missing values for `--searxng-url`, `--model`, `--webui-port`, `--searxng-port` exit 1.
- `--recommend` produces a "DETECTED SYSTEM" header, writes the catalogue, reports catalogue age, and does not overwrite an existing catalogue (test by corrupting the date line, re-running, and verifying it survives).
- `--status` shows Ollama, Open WebUI, and SearXNG lines.
- Catalogue format validation: 7 pipe-delimited fields, valid tag format (`[a-zA-Z0-9._:-]+`), VERIFIED column is `yes`/`no`, ARCH column is `moe`/`dense`, ROLE column is one of the known values, at least one verified daily driver.
- Regression guard: known-fictional tags (`qwen3.6:35b-a3b`, `gemma4:26b-a4b`, `llama3.3:70b`) do not appear in the script or the generated catalogue.
- Idempotency: `--recommend` produces identical output on consecutive runs.

### macOS test job (Apple Silicon, conditional)
- Only runs on pushes to `main` or manual `workflow_dispatch`, to conserve macOS runner minutes (billed at 10x rate).
- Same syntax check and shellcheck.
- `--recommend` detects real hardware: chip is not "unknown", memory > 0, disk > 0, a valid tier is assigned (Ultra/Max/Pro/Base), a bandwidth note is present, and at least one model is recommended.
- `--status` shows all three components.
- Catalogue is written and valid on macOS.

### CI pass gate job
- A synthetic job depending on `lint` and `test` (but not the conditional `test-macos`), used as the required status check for branch protection.
- Passes only when both core jobs succeed.

## 15. Extensions

<!-- Append refinements and new requirements below. Reference the section
     being amended, e.g. "§6: add a --dry-run mode that prints every
     command it would execute and exits." -->